int a;
float b;
bool c;
string s;

a = 3.14;         // ❌ float → int (запрещено)
b = "hello";      // ❌ string → float (нельзя)
c = 42;           // ❌ int → bool (нельзя)
s = true;         // ❌ bool → string (нельзя)

a = b;            // ❌ float → int (запрещено)

if (a) {
    write "Invalid if";
}

while ("loop") {
    write "Wrong while";
}

write a + s;      // ❌ int + string (нельзя, не конкатенация)
